program Program

    class A {
        int x[], y[];
        {
            int getGet()  A a, b, c; C c;{

            }
        }
    }

    const int pi = 3, e = 2;
    int meni, tebi, asd[];
    int be[];



    class B extend[s] A {
        int i; int x[], x=y;
        {
            int getValue(int a, bool b)  A a; C c;
            { return this.i + this.x[0] + a;  }
        }
    }

    class C extends B {
        A theA;
        string a;
    }
{
    int getValueA(int a)  A a;
    { return a + a;  }

    int getValueB(int a)  A a;
    { return a + a;  }

    int getValueC(int a)  A a;
    { return a + a;  }

    int getValueD(int a)  A a;
    { return  a;  }

    void main() A a; C c; int i; int x[];
    {
        a = new A;
        {
            a.x = new int[5];
            a.y = new int[5];
            c.theA = a;
        }
        c.x = new int[5];
        {
            x = new int[3];
            i = 0;
        }
        read()c.i);
        getValueD(10);
        if( i <= 89) print(67);

        if(isTrue[10]) {
            read(isTrue[10]);
        } else {
            if(print(isTrue[10]);
        }

        while( i == 5 ) {
            read(c.x[;,i]); read(c.theA.x[i]);
            i++;
        }
        print(c.getValue(c.theA.x[0]));
    }
}